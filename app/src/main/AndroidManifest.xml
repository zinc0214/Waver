<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zinc.berrybucket">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.zinc.berrybucket.MyBuryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BerryBucket">
        <activity
            android:name="com.zinc.berrybucket.presentation.HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.BerryBucket.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.zinc.berrybucket.presentation.detail.my.open.MyOpenDetailActivity"
            android:theme="@style/Theme.BerryBucket.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
    </application>
</manifest>